<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">QSS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% set current_route = app.request.attributes.get('_route') %}
            <li class="nav-item {% if current_route == 'author_index' %}active{% endif %}">
                <a class="nav-link" href="{{ url('author_index') }}">
                    {{ "Authors"|trans }}
                </a>
            </li>
            <li class="nav-item {% if current_route == 'book_index' %}active{% endif %}">
                <a class="nav-link" href="{{ url('book_index') }}">
                    {{ "Books"|trans }}
                </a>
            </li>
        </ul>
        <div class="form-inline my-2 my-lg-0">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ app.token.user }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ url('user_logout') }}">
                            {{ "Logout"|trans }}
                        </a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
